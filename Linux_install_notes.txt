## Notes from install process on Ubuntu Xenial:

Libraries SP needed to install on Ubuntu Xenial to get netDx to run
http://cran.utstat.utoronto.ca/

* setup password-less authentication.

Installing R 3.2.5 from source required the install of these packages:
use sudo apt-get install <packagename>
gcc
g++
gfortran
xorg

install java:
open-jre
install python:
python3

needed this for something, I forget what:
xorg-dev

Bioconductor needed these:
liblapack-dev
liblapack3
libopenblas-base
libopenblas-dev
——
install texlive. 
run mblastoma vignette. 
look at pdf.
then run lusc.
